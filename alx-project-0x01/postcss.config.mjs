// postcss.config.mjs - Fixed ES module syntax
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;


/* const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config; */
